# 施設予約システム 引き継ぎ資料

> **作成日**: 2026-07-26
> **対象**: CoreServer（b45.coreserver.jp）共有ホスティング環境

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| システム名 | 施設予約システム |
| 稼働URL | https://fbs.geo.jp/ |
| リポジトリ | https://github.com/kuwa2005/facility-booking-system |
| ブランチ | `cgi-mode-support`（本番ブランチ） |
| 技術スタック | Node.js / TypeScript / Express.js / EJS / MariaDB |
| デプロイ方式 | CGIモード（共有ホスティング対応） |

---

## 2. 環境情報

### 2.1 サーバー情報

| 項目 | 値 |
|------|-----|
| サーバー名 | CoreServer（b45.coreserver.jp） |
| OS | Ubuntu（カーネル 5.15.0-168-generic） |
| Webサーバー | Apache 2.4.67 |
| データベース | MariaDB 10.6.24 |
| GLIBC | 2.28 |
| アカウント | pcm |

### 2.2 パス情報

| 項目 | パス |
|------|------|
| ドキュメントルート | `/virtual/pcm/public_html/fbs.geo.jp/` |
| Node.js | `/virtual/pcm/.nvm/versions/node/v24.18.0/bin/node` |
| npm | `/virtual/pcm/.nvm/versions/node/v24.18.0/bin/npm` |
| Playwrightモジュール | `~/.local/lib/node_modules/` |
| Playwright共有ライブラリ | `~/.local/lib/playwright/` |
| Chromiumキャッシュ | `~/.cache/ms-playwright/` |
| Apacheエラーログ | `/usr/local/apache24/logs/error.log` |

### 2.3 データベース情報

| 項目 | 値 |
|------|-----|
| DB名 | pcm_fbs |
| ユーザー名 | pcm_fbs |
| パスワード | bbCE |
| ホスト | localhost |
| ポート | 3306 |

### 2.4 デモアカウント

| 種別 | メールアドレス | パスワード | 権限 |
|------|---------------|-----------|------|
| 管理者 | admin@facility.local | admin123 | admin |
| 管理者 | admin@example.com | admin123 | admin |
| 職員 | staff@facility.local | staff123 | staff |
| 職員 | staff2@facility.local | staff123 | staff |

---

## 3. ファイル構成

```
/virtual/pcm/public_html/fbs.geo.jp/
├── .env                    # 環境変数（DB接続情報等）
├── .htaccess               # Apache URLルーティング設定
├── cgi-bin/
│   ├── app.cgi             # シェルラッパー（Node.js起動用）
│   └── app.cjs             # CGIハンドラ（リクエスト処理）
├── src/
│   ├── cgi.ts              # CGIエントリーポイント（Express定義）
│   ├── server.ts           # 通常モード用サーバー（CGIでは未使用）
│   ├── controllers/        # コントローラー
│   ├── services/           # ビジネスロジック
│   ├── models/             # データアクセス層
│   ├── routes/             # ルート定義
│   ├── views/              # EJSテンプレート
│   └── middleware/          # ミドルウェア
├── dist/                   # TypeScriptビルド成果物
│   ├── cgi.js              # CGI用ビルド済みファイル
│   ├── views/              # EJSテンプレート（コピー）
│   └── ...
├── migrations/             # DBマイグレーション
├── public/                 # 静的ファイル
├── uploads/                # アップロードファイル
└── node_modules/           # 依存関係
```

---

## 4. デプロイ手順

### 4.1 初回デプロイ

```bash
# 1. ファイルコピー
cp -r /path/to/source/* /virtual/pcm/public_html/fbs.geo.jp/

# 2. .env作成
cat > /virtual/pcm/public_html/fbs.geo.jp/.env << 'EOF'
NODE_ENV=production
DB_HOST=localhost
DB_PORT=3306
DB_USER=pcm_fbs
DB_PASSWORD=bbCE
DB_NAME=pcm_fbs
JWT_SECRET=<ランダムな文字列>
APP_URL=https://fbs.geo.jp
EOF

# 3. 依存関係インストール
cd /virtual/pcm/public_html/fbs.geo.jp
NODE_OPTIONS="--max-old-space-size=512" npm install --omit=dev

# 4. ネイティブモジュール再ビルド
npm rebuild bcrypt
npm rebuild sharp

# 5. TypeScriptビルド
NODE_OPTIONS="--max-old-space-size=512" node node_modules/typescript/bin/tsc

# 6. viewsをdistにコピー
cp -r src/views dist/

# 7. マイグレーション実行
node dist/migrations/runner.js

# 8. CGIスクリプトに実行権限
chmod +x cgi-bin/*.cgi

# 9. uploadsディレクトリ作成
mkdir -p uploads
```

### 4.2 更新デプロイ

```bash
cd /virtual/pcm/public_html/fbs.geo.jp

# ファイル更新
cp -r /path/to/source/src .
cp -r /path/to/source/cgi-bin .

# ビルド
NODE_OPTIONS="--max-old-space-size=512" node node_modules/typescript/bin/tsc
cp -r src/views dist/
```

---

## 5. 動作確認

### 5.1 基本テスト

```bash
# ヘルスチェック
curl -s -o /dev/null -w "%{http_code}" https://fbs.geo.jp/health
# → 200

# トップページ
curl -s https://fbs.geo.jp/ | grep "<title>"
# → 施設予約システム（DEMO）

# ログインAPI
curl -s -X POST https://fbs.geo.jp/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@facility.local","password":"admin123"}'
# → Login successful
```

### 5.2 Playwrightテスト

```bash
NODE=/virtual/pcm/.nvm/versions/node/v24.18.0/bin/node

# Chromium起動テスト
LD_LIBRARY_PATH=~/.local/lib/playwright \
  ~/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell --version
# → Google Chrome for Testing 151.0.7922.34

# Playwrightテスト（Yahoo! JAPAN トピックス + スクショ）
LD_LIBRARY_PATH=~/.local/lib/playwright NODE_PATH=~/.local/lib/node_modules $NODE -e "
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.setViewportSize({ width: 1280, height: 900 });
  await page.goto('https://news.yahoo.co.jp/topics', { waitUntil: 'load', timeout: 30000 });
  await page.waitForTimeout(5000);
  await page.screenshot({ path: './yahoo-topics.png', fullPage: false });
  console.log('Screenshot saved');
  await browser.close();
})();
"
```

---

## 6. 重要な制約

### 6.1 CGIモードの制約

- **Node.jsは常駐できない**: リクエストごとにプロセス起動・終了
- **`node`コマンドが使えない**: フルパス指定が必須
- **`console.log()`が使えない**: stderrにリダイレクトが必要
- **express.json()が使えない**: CGI用ボディパーサーを自作

### 6.2 対応済みの問題

| 問題 | 解決策 |
|------|--------|
| `#!/usr/bin/env node` が使えない | シェルラッパーでフルパス指定 |
| `console.log()` がCGIヘッダーを壊す | stderrにリダイレクト |
| asyncミドルウェアのエラーが無視される | `Promise.resolve().catch(next)` でラップ |
| クッキーのpathが `/api/auth/` | `path: '/'` を明示指定 |
| Set-Cookieヘッダーが出力されない | CGIハンドラでヘッダー構築 |
| viewsがdistにない | `cp -r src/views dist/` |
| Playwrightが動かない | 共有ライブラリを手動ダウンロード |

---

## 7. ドキュメント一覧

| ドキュメント | 内容 |
|-------------|------|
| `docs/CGI_MODE.md` | CGIモードデプロイガイド |
| `docs/CoreServer用ノウハウ.md` | CoreServer環境でのNode.js CGI運用ノウハウ |
| `docs/機能一覧.md` | 全機能の詳細一覧 |
| `docs/SSL化の手順書.md` | SSL/TLS設定手順 |
| `docs/IMPLEMENTATION_STATUS.md` | 実装状況 |
| `docs/AUTH_FIX_SUMMARY.md` | 認証関連の修正履歴 |
| `docs/STAFF_LOGIN.md` | 職員ログイン機能 |
| `docs/SYSTEM_SETTINGS_DEBUG.md` | システム設定デバッグ |
| `引き継ぎ資料.md` | このドキュメント |

---

## 8. GitHub操作

### 8.1 認証情報

```bash
# gh CLI認証済み
gh auth status
# → Logged in to github.com account kuwa2005
```

### 8.2 ブランチ操作

```bash
# ブランチ一覧
git branch -a

# プッシュ
git push origin cgi-mode-support

# PR作成
gh pr create --title "..." --body "..."
```

---

## 9. トラブルシューティング

### 9.1 500エラー

```bash
tail -20 /usr/local/apache24/logs/error.log
```

### 9.2 ログイン失敗

```bash
# パスワードハッシュ確認
mysql -u pcm_fbs -pbbCE -h localhost pcm_fbs \
  -e "SELECT email, LEFT(password_hash, 30) FROM users WHERE email='admin@facility.local'"

# ハッシュ再生成
node -e "require('bcrypt').hash('admin123', 10).then(h => console.log(h))"
```

### 9.3 Playwrightが動かない

```bash
# ライブラリ確認
ldd ~/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell 2>&1 | grep "not found"
```

---

## 10. この環境特有の留意点

### 10.1 メモリ制限

- `npm install` がメモリ不足で失敗する → `NODE_OPTIONS="--max-old-space-size=512"` が必須
- `npm rebuild` も同様にメモリ不足の可能性あり
- CGIスクリプトでも `--max-old-space-size=256` を設定

### 10.2 ビルドとデプロイのタイミング

- **TypeScriptビルド後に `src/views` を `dist/` にコピー**しないとEJSテンプレートが見つからない
- `tsc` はviewsをコピーしない → 手動で `cp -r src/views dist/` が必要
- **CGIモードなのでサーバー再起動不要**、ファイル変更即反映

### 10.3 パスの扱い

- `node` コマンドは `PATH` に含まれない → **必ずフルパス使用**
- `npx` も使えない場合がある → `node node_modules/typescript/bin/tsc` のように直接実行
- `#!/usr/bin/env node` はCGI環境で使えない → シェルラッパーでフルパス指定

### 10.4 データベース

- マイグレーションはスキーマ変更のみ → 既存データは保持される
- パスワードハッシュが不正な場合がある → bcryptで再生成が必要
- `system_settings` テーブルの設定は `information_schema` で初期化済みチェック

### 10.5 クッキーと認証

- `res.cookie()` に `path: '/'` を**必ず指定**（省略すると `/api/auth/` になる）
- CGIモードでは `express.json()` が使えない → カスタムボディパーサーを使用
- Set-CookieヘッダーはCGIハンドラで手動構築

### 10.6 Playwright

- `npx playwright install-deps` は使えない（sudo必要）
- 共有ライブラリはUbuntuパッケージから手動ダウンロード
- `cp -Pn` でシンボリックリンクが切れる → 手動でリンク再作成が必要
- GLIBC 2.28 対応のパッケージを使用（focal/bionic）
- `NODE_PATH=~/.local/lib/node_modules` が必要

### 10.7 静的ファイル

- `.htaccess` で `.css/.js/.svg/.png/.jpg` をApache直接配信に設定済み
- CGIに回すとパフォーマンスが落ちる → 静的ファイルはスキップ

### 10.8 セキュリティ

- **デモモード**: メール送信・決済は実際に行わない
- JWT秘密鍵は `.env` に設定 → 本番では必ず変更
- デフォルト管理者パスワード `admin123` → 本番では必ず変更
- レート制限は現在無効化済み（デモ用）

---

## 11. よく使うコマンド

```bash
# デプロイ先に移動
cd /virtual/pcm/public_html/fbs.geo.jp

# TypeScriptビルド
NODE_OPTIONS="--max-old-space-size=512" node node_modules/typescript/bin/tsc

# viewsコピー
cp -r src/views dist/

# サーバー再起動（CGIモードなので不要、即反映）

# テスト
curl -s -o /dev/null -w "%{http_code}" https://fbs.geo.jp/health
```
